type Query {
  users: [User!]!,
  user(id: ID!): User,
}

type User {
  id: ID!
  name: String!
  email: String
  birthDate: Int
  photo: String
}

type LoginResult {
  token: String!
}

type Mutation {
  login(username: String!, password: String!): LoginResult
}
